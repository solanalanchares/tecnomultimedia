void claseUno() {
  momento = "enClase";
  image(enclase, 0, 0);
  textAlign(LEFT);
  fill(255, 170);
  rect(30, 395, 730, 140, 10);
  fill(0);
  text("Profesor: Bienvenides a Tecno I! Hoy vamos a conocer de qué se trata la materia y\nde qué se trata processing, la herramienta que vamos a estar usando durante las\npróximas clases y para trabajar en sus entregas.\nPor ser nuestra primera clase vamos a aprender a dibujar algunas figuras\nbásicas por medio de código.", 45, 420);
  botonSig();
}

void claseDos() {
  momento = "enClase";
  image(enclase, 0, 0);
  textAlign(LEFT);
  fill(255, 170);
  rect(30, 400, 730, 135, 10);
  fill(0);
  text("Profesor: Buenos días chiques! Ahora que ya saben hacer algunos dibujos básicos\npor medio de código, vamos a ver de qué se tratan las variables.\nEl uso de variables les va a permitir almacenar información, para después usarla\nfácilmente en sus creaciones.", 40, 430);
  botonSig();
}

void claseTres() {
  momento = "enClase";
  image(enclase, 0, 0);
  textAlign(LEFT);
  fill(255, 170);
  rect(30, 400, 730, 150, 10);
  fill(0);
  text("Profesor: Buenos días chiques! Ya conocen los distintos tipos de variables y cómo\nconstruir sus propias variables.\nHoy vamos a ver como utilizar imágenes y fuentes para poder acompañar sus\ncreaciones y también vamos a repasar el uso de variables.", 40, 430);
  botonSig();
}

void claseCuatro() {
  momento = "enClase";
  image(enclase, 0, 0);
  textAlign(LEFT);
  fill(255, 170);
  rect(30, 400, 730, 140, 10);
  fill(0);
  text("Profesor: Buenos días chiques! Para la clase de hoy tenemos como tema estructuras\ncondicionales, vamos a aprender como construir y usar un if. Estas estructuras\npermiten evaluar una condición y en base a esto ejecutar o no un bloque de código\nespecífico.", 40, 430);
  botonSig();
}

void claseCinco() {
  momento = "enClase";
  image(enclase, 0, 0);
  textAlign(LEFT);
  fill(255, 170);
  rect(30, 400, 730, 140, 10);
  fill(0);
  text("Profesor: Buenos días chiques! Esta es nuestra última clase, así que vamos a\nrepasar los temas visto y conoceremos el ciclo for. Este ciclo te va a permitir\ngenerar la repetición de un conjunto de reglas las veces que lo consideres necesario.", 40, 430);
  botonSig();
}
